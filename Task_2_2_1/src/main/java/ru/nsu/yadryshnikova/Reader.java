package ru.nsu.yadryshnikova;

import java.io.File;
import java.io.IOException;
import java.util.Arrays;
import java.util.List;


public class Reader {
  /*  List<BakersInfo> readBakers(File fileBakers) {
        try {
            ObjectMapper mapper = new ObjectMapper();
            return Arrays.asList(mapper.readValue(fileBakers, BakersInfo[].class));
        } catch (IOException e) {
            e.printStackTrace();
        }

        return null;
    }

    List<DeliverymanInfo> readDeliveryWorkers(File fileDeliveryWorkers) {
        try {
            ObjectMapper mapper = new ObjectMapper();
            return Arrays.asList(mapper.readValue(fileDeliveryWorkers, DeliverymanInfo[].class));
        } catch (IOException e) {
            e.printStackTrace();
        }

        return null;
    } */
}
